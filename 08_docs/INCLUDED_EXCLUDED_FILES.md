# Included and excluded files

Included: current PSCI formulas/matrix and the three frozen extraction-lineage scripts; topology, phenotype-rank and MD-QC evidence; Literature V3.2 prior inputs; 24 Internal16 pairs; frozen M0–M3 predictions, metrics and fold artifacts; negative controls/ablations cited by the computational narrative; manuscript computational source tables; public wrappers and tests. In the archived extraction scripts, only machine-specific source/GROMACS constants were replaced by `PSCI_MD_SOURCE_ROOT`/`GMX_BIN` environment variables; scientific logic is unchanged.

Excluded: raw production XTC/TPR and large structures; PDFs; ESM caches; model checkpoints; debug logs; notebooks; temporary/cache files; obsolete multiplicative PSCI and SAE prototypes; deprecated Public54/placeholder predictions; unused exploratory models; manuscript prose and figures. Exclusion is by relevance/size, never result favorability.

No authoritative experimental replicate-level table was available. It was not synthesized. RHLARHLB_T04 remains numeric-missing.
